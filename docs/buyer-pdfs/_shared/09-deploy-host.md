# Chapter — Deploy: Docker, systemd, GCE cloud-init, Cloud Run

> TODO — this chapter is a stub. Full content shipping in the next buyer-PDF iteration. The shared API-strategy chapter (`01-api-strategy.md`) and GCP SA chapter (`02-gcp-service-account.md`) are the current ship-ready content.

For now, the SKU-specific AGENTS.md at the repo root covers the install path. The buyer PDF will absorb that content + this chapter's deeper coverage in the next release.

